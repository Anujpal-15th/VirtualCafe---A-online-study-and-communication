# Virtual Cafe - Requirements
# Core Django
Django==4.2.7

# Image handling for user avatars
Pillow==11.1.0

# Django Channels for WebSocket support
channels==4.0.0
channels-redis==4.1.0

# ASGI server
daphne==4.0.0

# Redis client for channel layer
redis==5.0.1

# Background task scheduler for room cleanup
APScheduler==3.10.4

# PostgreSQL support (optional for production)
psycopg[binary]==3.3.2

# Environment variables management
python-dotenv==1.0.0

# Google Generative AI (Gemini) - Updated package
google-genai>=1.61.0
